"""Desktop control panel for the standalone ASTAR SECS/GEM simulator.

Deliberately separate from `gui`, which drives the middleware: the
simulator ships as its own installer and must be configurable on a
machine where no middleware is installed.
"""

__version__ = "1.0.0"
